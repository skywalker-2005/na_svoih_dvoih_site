import './style.css'

const Rules = ({rules}) => {
  return (
    <a href={rules} target="_blank" rel="noopener noreferrer" className="Rules">Правила нахождения на территории</a>
  );
}
 
export default Rules;